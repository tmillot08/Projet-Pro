{% extends 'base.html.twig' %}

{% block title %}Modification secretaire{% endblock %}


{% block body %}

<main class="gestion">

        <div class="filterbis">
    
            <h3 style="text-align:center; margin-top: 30px; ">Modification de {{secretary.name}} {{secretary.fName}}</h3>
        <section>
        {{ form_start(form, {'attr' : {'class':'form col'}})}}
        <div class="row">
                {{ form_widget(form.name, {'attr' : {'class':'lineInput', 'placeholder': 'Nom'}})}}
        </div>
        <div class="row">
                {{ form_widget(form.fName, {'attr' : {'class':'lineInput', 'placeholder': 'Prenom'}})}}
        </div>
        <div class="row">
                {{ form_widget(form.mail, {'attr' : {'class':'lineInput', 'placeholder': 'Mail'}})}}
        </div>
        <div class="rowsubmit" style="margin: 30px 0 30px 0  ">
        <button type="submit" class="inputsubmit">Modifier</button>
        </div>
        {{ form_end(form)}}
        </section>
        </div>
    </main>

{% endblock %}